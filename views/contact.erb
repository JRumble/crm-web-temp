
    <h1>The Best Fans in the WORLD!</h1>

    <ul>
      <% @contacts.each do |contact| %>
        <li>
          <a href="/contacts/<%= contact.id %>"><%= contact.first_name %> <%= contact.last_name %></a>
        </li>
        <li>
          <%= contact.email %> 
        </li>
        <li>
          <%= contact.note %>
        </ll>



      <% end %>

    </ul>
  

